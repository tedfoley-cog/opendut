tls:
  options:
    default:
      clientAuth:
        caFiles:
          - '{{ env "OPENDUT_TRAEFIK_CLIENT_AUTH_CA_PATH" }}'
        clientAuthType: '{{ env "OPENDUT_TRAEFIK_CLIENT_AUTH_TYPE" }}'
      ocsp:
    no-mtls:
      clientAuth:
        clientAuthType: NoClientCert
